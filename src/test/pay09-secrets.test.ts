import { describe, it } from 'vitest'

describe('PAY-09 — PayFast secrets stay server-side; webhook endpoint is public', () => {
  it.todo('PAYFAST_PASSPHRASE does not appear in any src/ TypeScript file')
  it.todo('payfast-itn config.toml sets verify_jwt = false')
})
