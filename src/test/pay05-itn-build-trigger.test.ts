import { describe, it } from 'vitest'

describe('PAY-05 — Initial payment COMPLETE triggers site build via generate-site', () => {
  it.todo('payfast-itn source invokes generate-site on initial COMPLETE')
  it.todo('OnboardingPage source does NOT invoke build-site')
})
