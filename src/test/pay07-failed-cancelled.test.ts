import { describe, it } from 'vitest'

describe('PAY-07 — Failed payment retry and cancellation suspend site', () => {
  it.todo('payfast-itn source handles FAILED status')
  it.todo('payfast-itn source invokes suspend-site on CANCELLED')
})
