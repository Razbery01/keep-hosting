// Stub — real test body is filled in by Plan 04 (see 01-VALIDATION.md per-task map)
import { describe, it } from 'vitest'

describe('Admin-scoped queries reject non-admin server-side', () => {
  it.todo('behavior to be implemented in Plan 04 SEC-02')
})
