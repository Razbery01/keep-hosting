// Stub — real test body is filled in by Plan 04 (see 01-VALIDATION.md per-task map)
// NOTE: this file is excluded from the default Vitest run (see vite.config.ts exclude list)
// Run via `npm run test:rls` (Plan 04) with a real Supabase test project
import { describe, it } from 'vitest'

describe('Non-admin RLS isolation across PII tables', () => {
  it.todo('behavior to be implemented in Plan 04 SEC-05')
})
