import { describe, it } from 'vitest'

describe('prompts — CODE_AGENT_SYSTEM prompt', () => {
  it.todo('CODE_AGENT_SYSTEM contains mobile-responsive enforcement line')
  it.todo('CODE_AGENT_SYSTEM requires <meta name=viewport> in every HTML file')
  it.todo('buildCodeAgentPrompt injects {industry_hints} slot with the provided hints')
  it.todo('CODE_AGENT_SYSTEM contains deliver_site_files tool name reference')
  it.todo('buildCodeAgentPrompt accepts businessName, industry, and package tier arguments')
})
