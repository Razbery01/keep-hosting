---
phase: 03-deployment-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-04
  - DEPLOY-05

must_haves:
  truths:
    - "Netlify team account exists with NETLIFY_PAT and NETLIFY_TEAM_SLUG set as Supabase secrets"
    - "Migration 005 has been applied to the live database"
    - "A real site deploy completes via deploy-site and is accessible at the Netlify URL"
    - "Credit-based pricing tier has been confirmed and budgeted"
  artifacts: []
  key_links: []
---

<objective>
Human-verified deployment: create the Netlify team account, set Edge Function secrets, apply migration 005, deploy Edge Functions, and verify a real end-to-end deploy + suspend + reactivate cycle.

Purpose: The code from Plans 01-02 is complete but untested against real Netlify APIs. This plan confirms the integration works with real credentials, real deploys, and real Netlify responses. It also confirms the Netlify pricing tier (credit-based vs legacy) which is a potential launch blocker.

Output: Verified working deploy pipeline with real Netlify team account.
</objective>

<execution_context>
@/Users/kirkmaddocks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kirkmaddocks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deployment-pipeline/03-CONTEXT.md
@.planning/phases/03-deployment-pipeline/03-RESEARCH.md
@.planning/phases/03-deployment-pipeline/03-01-SUMMARY.md
@.planning/phases/03-deployment-pipeline/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration 005 + deploy all Edge Functions</name>
  <files></files>
  <action>
1. Apply migration 005 to the live Supabase project:
   ```
   cd /Users/kirkmaddocks/keep-hosting && npx supabase db push
   ```
   This extends the `build_status` CHECK constraint and adds the deploy-related indexes.

2. Deploy all modified and new Edge Functions to Supabase:
   ```
   npx supabase functions deploy deploy-site
   npx supabase functions deploy suspend-site
   npx supabase functions deploy reactivate-site
   npx supabase functions deploy persist-files
   npx supabase functions deploy build-orchestrator
   ```

3. Verify functions are listed:
   ```
   npx supabase functions list
   ```

Note: The Netlify secrets (NETLIFY_PAT, NETLIFY_TEAM_SLUG) must be set before deploy-site can work. That happens in the checkpoint task below.
  </action>
  <verify>
    <automated>cd /Users/kirkmaddocks/keep-hosting && npx supabase functions list 2>&1 | grep -E 'deploy-site|suspend-site|reactivate-site|persist-files|build-orchestrator'</automated>
  </verify>
  <done>Migration 005 applied. All 5 Edge Functions deployed and visible in function list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Netlify account setup + real deploy/suspend/reactivate cycle</name>
  <action>
Human creates Netlify team account, sets secrets, and verifies the full deploy pipeline works against real Netlify APIs. Claude presents verification steps; human executes and reports results.
  </action>
  <verify>Human confirms all 5 steps below pass.</verify>
  <done>Real site deployed to kh-{uuid8}.netlify.app, suspended with placeholder, reactivated with original content. Netlify pricing tier documented.</done>
  <what-built>
Complete Netlify deploy pipeline: deploy-site (zip-deploy + rate limit + polling), suspend-site (placeholder deploy), reactivate-site (Storage re-deploy), persist-files chain, build-orchestrator retry extension, migration 005, and all 6 DEPLOY test files.
  </what-built>
  <how-to-verify>

### Step 1: Create Netlify Team Account + Set Secrets

1. Go to https://app.netlify.com and create a **dedicated Team account** for keep-hosting (or use existing one).
2. Note the **team slug** from the URL: `app.netlify.com/teams/{TEAM_SLUG}/...`
3. Go to **User Settings > Applications > Personal Access Tokens** and create a new token with deploy scope. Copy the token.
4. **CRITICAL: Check your pricing tier.**
   - Go to **Team Settings > Billing**.
   - If your account was created **after September 4, 2025**, you are on credit-based pricing: 300 credits/month, 15 credits per production deploy = only ~20 free deploys/month.
   - If your account is on a **legacy plan** (created before Sep 4, 2025), deploys are unlimited on the free tier.
   - If credit-based: consider upgrading to Pro ($19/mo, 1000 credits = ~66 deploys/month) before launch.
   - **Report your pricing tier** when you resume.

5. Set the secrets in Supabase:
   ```
   npx supabase secrets set NETLIFY_PAT="{your_token}"
   npx supabase secrets set NETLIFY_TEAM_SLUG="{your_team_slug}"
   ```

### Step 2: Test Real Deploy

1. Find an existing `client_sites` row with `build_status = 'generated'` and files in Storage. (Or create one by running the generation pipeline on a test order.)
2. Invoke deploy-site directly:
   ```
   npx supabase functions invoke deploy-site --body '{"siteId": "{SITE_ID}"}'
   ```
3. Check the response -- should include `success: true` and a `netlify_url`.
4. **Visit the Netlify URL** -- the generated site should be live and accessible.
5. Check `client_sites` in the database -- `build_status` should be `deployed`, `netlify_site_id` and `netlify_url` populated.
6. Check `build_events` -- should have `deploy_start`, `deploy_done` events.

### Step 3: Test Suspend

1. Using the same siteId from Step 2:
   ```
   npx supabase functions invoke suspend-site --body '{"siteId": "{SITE_ID}"}'
   ```
2. **Visit the Netlify URL** -- should now show the branded "temporarily unavailable" placeholder page.
3. Check `client_sites` -- `build_status` should be `suspended`.
4. Check the linked `orders` row -- `status` should be `suspended`.

### Step 4: Test Reactivate

1. Using the same siteId:
   ```
   npx supabase functions invoke reactivate-site --body '{"siteId": "{SITE_ID}"}'
   ```
2. **Visit the Netlify URL** -- should now show the original generated site content (not the placeholder).
3. Check `client_sites` -- `build_status` should be `deployed`.

### Step 5: Verify Netlify Dashboard

1. Go to your Netlify team dashboard.
2. Confirm the site exists with name `kh-{first8chars}`.
3. Confirm 3 deploys are visible (initial deploy, suspend placeholder, reactivate).

  </how-to-verify>
  <resume-signal>
Report:
1. "approved" or describe issues
2. Netlify pricing tier: "legacy" or "credit-based (X credits/month)"
3. Team slug value (for records)
  </resume-signal>
</task>

</tasks>

<verification>
- Migration 005 applied (build_status CHECK includes deploying/deployed/deploy_failed/suspending)
- All 5 Edge Functions deployed and listed
- Netlify secrets set (NETLIFY_PAT, NETLIFY_TEAM_SLUG)
- Real deploy-site invocation returns success with netlify_url
- Real suspend-site invocation shows placeholder page
- Real reactivate-site invocation restores original content
- Netlify dashboard shows kh-{uuid8} site with 3 deploys
</verification>

<success_criteria>
1. Netlify team account exists and pricing tier is documented
2. NETLIFY_PAT and NETLIFY_TEAM_SLUG are set as Supabase Edge Function secrets
3. A real site has been deployed, suspended, and reactivated successfully
4. The deployed site is accessible at a kh-{uuid8}.netlify.app URL
5. The suspended site shows the branded placeholder page
6. The reactivated site shows the original generated content
</success_criteria>

<output>
After completion, create `.planning/phases/03-deployment-pipeline/03-03-SUMMARY.md`
</output>
