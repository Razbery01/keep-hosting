---
phase: 03-deployment-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06

user_setup:
  - service: netlify
    why: "Site hosting and deploy target"
    env_vars:
      - name: NETLIFY_PAT
        source: "Netlify Dashboard -> User Settings -> Applications -> Personal Access Tokens -> New access token (name: keep-hosting, expiry: no expiry)"
      - name: NETLIFY_TEAM_SLUG
        source: "Netlify Dashboard -> Team settings -> General -> Team slug (visible in URL: app.netlify.com/teams/{slug})"
    dashboard_config:
      - task: "Create dedicated Netlify team account (or use existing)"
        location: "Netlify Dashboard -> Create new team (or use existing team)"
      - task: "Verify account age (pre-Sep 2025 = legacy pricing = unlimited deploys; post-Sep 2025 = credit pricing = 20 free deploys/month)"
        location: "Netlify Dashboard -> Billing"
      - task: "Note: if credit-based pricing, consider Pro plan ($20/mo) for 200 deploys/month"
        location: "Netlify Dashboard -> Billing -> Upgrade"

must_haves:
  truths:
    - "Migration 005 applied successfully to the live Supabase database"
    - "All three new Edge Functions deployed and accessible"
    - "NETLIFY_PAT and NETLIFY_TEAM_SLUG secrets set on Supabase"
    - "GITHUB_PAT secret removed from Supabase"
    - "A real test site deploys to Netlify and is accessible via HTTPS"
    - "Suspend-site deploys placeholder page and site shows suspension message"
    - "Reactivate-site restores original content from Storage"
    - "Netlify pricing tier confirmed and budgeted"
  artifacts: []
  key_links:
    - from: "Supabase Edge Functions"
      to: "Netlify API"
      via: "NETLIFY_PAT secret"
      pattern: "deploy-site invocable and returns success"
---

<objective>
Deploy and verify: apply migration 005, deploy all Edge Functions, configure Netlify secrets, remove GITHUB_PAT, and run a full real-world deploy/suspend/reactivate cycle with human verification.

Purpose: This is the go/no-go checkpoint for Phase 3. Everything built in Plans 01-02 is validated against real infrastructure. The user confirms Netlify account setup, pricing tier, and the actual deploy result.

Output: Live infrastructure ready for Phase 4 payment integration to trigger deploys
</objective>

<execution_context>
@/Users/kirkmaddocks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kirkmaddocks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deployment-pipeline/03-CONTEXT.md
@.planning/phases/03-deployment-pipeline/03-RESEARCH.md
@.planning/phases/03-deployment-pipeline/03-01-SUMMARY.md
@.planning/phases/03-deployment-pipeline/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration 005 + deploy Edge Functions + set/unset secrets</name>
  <files></files>
  <action>
Run the following commands in sequence. If any step fails, stop and report the error.

1. **Apply migration 005:**
   ```bash
   cd /Users/kirkmaddocks/keep-hosting && npx supabase db push
   ```
   Verify output confirms `005_deploy_pipeline.sql` applied. If it reports "already applied", that's fine — migration is idempotent.

2. **Deploy all new + updated Edge Functions:**
   ```bash
   npx supabase functions deploy deploy-site --no-verify-jwt
   npx supabase functions deploy suspend-site --no-verify-jwt
   npx supabase functions deploy reactivate-site --no-verify-jwt
   npx supabase functions deploy persist-files --no-verify-jwt
   npx supabase functions deploy build-orchestrator --no-verify-jwt
   ```
   `--no-verify-jwt` because these are called by other Edge Functions with the service role key, not by end-user JWTs.

3. **Set Netlify secrets** (user must have created PAT and noted team slug first — checkpoint will verify):
   ```bash
   npx supabase secrets set NETLIFY_PAT="<from user>"
   npx supabase secrets set NETLIFY_TEAM_SLUG="<from user>"
   ```
   **NOTE:** Do NOT hardcode actual secret values. Prompt the user for them or read from .env if available.

4. **Remove GITHUB_PAT secret:**
   ```bash
   npx supabase secrets unset GITHUB_PAT
   ```
   This is safe — confirmed via grep that no current Edge Function source references GITHUB_PAT.

5. **Verify secrets are set:**
   ```bash
   npx supabase secrets list
   ```
   Should show NETLIFY_PAT and NETLIFY_TEAM_SLUG present, GITHUB_PAT absent.

6. **Run full test suite** to confirm no regressions:
   ```bash
   npm run test:ci
   ```
  </action>
  <verify>
    <automated>npx supabase functions list 2>&1 | grep -c "deploy-site\|suspend-site\|reactivate-site"</automated>
  </verify>
  <done>Migration 005 applied, all 5 Edge Functions deployed (3 new + 2 updated), NETLIFY_PAT and NETLIFY_TEAM_SLUG set, GITHUB_PAT removed, test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify real Netlify deploy/suspend/reactivate cycle + confirm pricing</name>
  <action>
Human verifies the complete deploy pipeline against real Netlify infrastructure. This checkpoint covers Netlify account setup, pricing confirmation, and a full deploy/suspend/reactivate cycle.
  </action>
  <what-built>
Complete Netlify deploy pipeline: deploy-site creates sites and zip-deploys, suspend-site deploys placeholder, reactivate-site restores from Storage. Migration 005 applied, Edge Functions deployed, secrets configured, GITHUB_PAT removed.

**Pricing alert:** If your Netlify account was created AFTER September 4, 2025, you are on credit-based pricing: 300 free credits/month = ~20 deploys. Each deploy (including suspend placeholder deploys) costs 15 credits. Consider the Pro plan ($20/mo = 200 deploys/month) if you expect more than 20 customers.

**Alternative for suspension (saves credits):** Research confirmed that `PUT /api/v1/sites/{site_id}/disable` exists and costs 0 credits. The current implementation uses placeholder-deploy per your CONTEXT.md decision (branded UX). If credit budget is a concern, we can switch to the disable endpoint in a follow-up — just say the word.
  </what-built>
  <how-to-verify>
**Pre-flight (Netlify account):**
1. Confirm you have a Netlify team account (new or existing)
2. Note the team slug from the dashboard URL: `app.netlify.com/teams/{slug}`
3. Create a Personal Access Token: User Settings > Applications > Personal Access Tokens > New
4. Confirm the secrets were set correctly: `npx supabase secrets list` shows NETLIFY_PAT and NETLIFY_TEAM_SLUG
5. Check your Netlify billing page: Dashboard > Billing > note whether you see "credits" or "legacy" pricing

**Test 1 — Real deploy:**
Pick a test client_site that has `build_status = 'generated'` and files in Storage. Invoke:
```bash
npx supabase functions invoke deploy-site --body '{"siteId":"<test-site-id>"}'
```
Expected: Response includes success. Check Netlify dashboard — a new site named `kh-{8chars}` should appear. Visit the `netlify_url` — the generated site should load.

**Test 2 — Suspend:**
```bash
npx supabase functions invoke suspend-site --body '{"siteId":"<same-site-id>"}'
```
Expected: Visit the same Netlify URL — should now show the branded "temporarily unavailable" placeholder page with the keep-hosting payment CTA.

**Test 3 — Reactivate:**
```bash
npx supabase functions invoke reactivate-site --body '{"siteId":"<same-site-id>"}'
```
Expected: Visit the same Netlify URL — original site content restored. build_status back to 'deployed' in the database.

**Test 4 — Dashboard:**
Visit the keep-hosting dashboard (as the test user). Confirm the build progress bar shows the updated pipeline steps (no "Saving files"/"GitHub" step, replaced with "Deploying site" / "Site deployed").

**Test 5 — Build events:**
Check build_events table for the test site — should have deploy_start, deploy_done, and possibly deploy_quota_warning events.
  </how-to-verify>
  <verify>Human confirms all 5 tests pass and pricing tier is acceptable</verify>
  <done>All tests pass: real site deployed to Netlify, suspend shows placeholder, reactivate restores content, dashboard shows correct steps, build_events logged. Netlify pricing confirmed. User approved.</done>
  <resume-signal>
Type "approved" if all 5 tests pass. If any issues, describe what happened and which test failed. If credit pricing is a concern, mention "switch to disable endpoint" and we can create a quick follow-up plan.
  </resume-signal>
</task>

</tasks>

<verification>
- Migration 005 confirmed applied (build_status CHECK includes deploying/deployed)
- All 3 new Edge Functions visible in `supabase functions list`
- NETLIFY_PAT + NETLIFY_TEAM_SLUG secrets confirmed set
- GITHUB_PAT confirmed removed
- Real deploy produced a live Netlify site accessible via HTTPS
- Suspend replaced content with branded placeholder
- Reactivate restored original content
- Dashboard renders correct pipeline steps
- build_events table shows deploy lifecycle events
- Full test suite green
</verification>

<success_criteria>
1. A real generated site deploys to Netlify via zip-deploy and is accessible at its kh-{uuid8}.netlify.app URL
2. Suspending the site shows the branded placeholder page (not a Netlify error page)
3. Reactivating the site restores original content from Storage without calling Claude
4. Netlify pricing tier is confirmed and acceptable for launch volume
5. GITHUB_PAT is removed from Supabase secrets
6. Dashboard UI shows correct deploy pipeline steps
7. User has approved the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-deployment-pipeline/03-03-SUMMARY.md`
</output>
