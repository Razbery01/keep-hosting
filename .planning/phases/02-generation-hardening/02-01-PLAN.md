---
phase: 02-generation-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/functions/_shared/html-scanner.test.ts
  - supabase/functions/_shared/cost-calc.test.ts
  - supabase/functions/_shared/industry-hints.test.ts
  - supabase/functions/_shared/prompts.test.ts
  - src/test/gen01.test.ts
  - src/test/gen02.test.ts
  - src/test/gen03.test.ts
  - src/test/gen05.test.ts
  - src/test/gen08.test.ts
  - src/test/gen09.test.ts
  - src/test/cust05.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "npm run test:ci exits zero after Plan 01 commits (all stubs use it.todo() or minimal green assertions)"
    - "Every Wave 1+ task in Phase 2 has a concrete test file to target with its <automated> verify"
    - "No stub imports production code that does not yet exist (would break import resolution)"
  artifacts:
    - path: "supabase/functions/_shared/html-scanner.test.ts"
      provides: "Target for GEN-04 viewport meta + mobile warning tests"
      contains: "describe.*html-scanner"
    - path: "supabase/functions/_shared/cost-calc.test.ts"
      provides: "Target for GEN-07 cost calculation tests"
      contains: "describe.*cost-calc"
    - path: "supabase/functions/_shared/industry-hints.test.ts"
      provides: "Target for GEN-06 industry hint map tests"
      contains: "describe.*industry-hints"
    - path: "supabase/functions/_shared/prompts.test.ts"
      provides: "Target for CODE_AGENT_SYSTEM + hint injection tests"
      contains: "describe.*prompts"
    - path: "src/test/gen01.test.ts"
      provides: "Target for tool_choice / tool_use extraction test"
    - path: "src/test/gen02.test.ts"
      provides: "Target for browser-bundle scan (no VITE_ANTHROPIC)"
    - path: "src/test/gen03.test.ts"
      provides: "Target for retry ladder + per-package token cap tests"
    - path: "src/test/gen05.test.ts"
      provides: "Target for queue state transition tests"
    - path: "src/test/gen08.test.ts"
      provides: "Target for Edge Function split existence tests"
    - path: "src/test/gen09.test.ts"
      provides: "Target for useBuildStatus Realtime hook tests"
    - path: "src/test/cust05.test.ts"
      provides: "Target for OnboardingPage slow-signUp regression test"
  key_links:
    - from: "vite.config.ts test.include"
      to: "src/**/*.{test,spec}.{ts,tsx} and supabase/functions/**/*.{test,spec}.{ts,tsx}"
      via: "glob matching"
      pattern: "src/\\*\\*.*supabase/functions"
---

<objective>
Create the 11 Vitest stub files listed in 02-VALIDATION.md so that every Wave 1+ task in Phase 2 has a concrete test file to target with its `<automated>` verify command. This is scaffolding only — no production code is written in this plan.

Purpose: Enforce the Nyquist sampling contract. Without stubs in place, Wave 1 tasks cannot declare automated verifiers and would violate the "no 3 consecutive tasks without automated verification" rule.

Output: 11 test files using `it.todo()` or trivial green assertions so `npm run test:ci` continues to pass on every intermediate commit.
</objective>

<execution_context>
@/Users/kirkmaddocks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kirkmaddocks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-generation-hardening/02-VALIDATION.md
@.planning/phases/02-generation-hardening/02-CONTEXT.md
@vite.config.ts
@src/test/setup.ts

<interfaces>
<!-- Vitest test-harness contract (installed + wired in Phase 1 — do NOT touch package.json or vite.config.ts) -->

From vite.config.ts:
```typescript
// test.include matches: 'src/**/*.{test,spec}.{ts,tsx}'
//                       'supabase/functions/**/*.{test,spec}.{ts,tsx}'
// environment: 'jsdom'
// fileParallelism: false (sequential)
// setupFiles: ['./src/test/setup.ts']
```

From src/test/setup.ts:
```typescript
import '@testing-library/jest-dom/vitest'
```

Vitest v4 API usage — stubs must compile without errors:
```typescript
import { describe, it, expect } from 'vitest'

describe('feature', () => {
  it.todo('will do the thing')
  // OR a trivially-green assertion:
  it('placeholder', () => { expect(true).toBe(true) })
})
```

From Phase 1 precedent (supabase/functions/__tests__/htmlScan.test.ts, promptSanitize.test.ts):
- Tests of pure-TS _shared/ modules are placed ALONGSIDE the module (e.g. supabase/functions/_shared/html-scanner.test.ts)
- Tests of product logic live in src/test/*.test.ts
- Both patterns work because vite.config.ts test.include matches both directories
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Edge-Function-adjacent shared-module stubs (4 files)</name>
  <files>
    supabase/functions/_shared/html-scanner.test.ts,
    supabase/functions/_shared/cost-calc.test.ts,
    supabase/functions/_shared/industry-hints.test.ts,
    supabase/functions/_shared/prompts.test.ts
  </files>
  <action>
Create four Vitest stub files in `supabase/functions/_shared/`. Each file is ~15–25 lines.

IMPORTANT: Do NOT import the production modules yet — they don't exist (cost-calc.ts, industry-hints.ts, prompts.ts) or would require changes not yet made (html-scanner.ts extension). Use pure `it.todo()` calls so the test discovers but does not execute.

1. `html-scanner.test.ts`:
   - `import { describe, it } from 'vitest'`
   - `describe('html-scanner — GEN-04 viewport meta', () => { it.todo('fails when <meta name="viewport"> is missing') })`
   - `describe('html-scanner — GEN-04 mobile warnings', () => { it.todo('reports hardcoded px widths on container selectors as soft warnings') })`
   - Leave a comment: `// Note: existing htmlScan.test.ts in supabase/functions/__tests__/ continues to cover the SEC-07 baseline checks. This file extends with GEN-04 behaviors added in Plan 02.`

2. `cost-calc.test.ts`:
   - Stubs: `it.todo('calcCostUsd computes input+output+cache costs at Sonnet 4.6 rates')`
   - `it.todo('usdToZar applies hardcoded 18.85 rate and rounds to 2 decimals')`
   - `it.todo('SONNET_4_6_PRICING constants match 02-RESEARCH.md source')`

3. `industry-hints.test.ts`:
   - Stubs: `it.todo('returns hints for plumber')`, `it.todo('returns hints for electrician')`, `it.todo('returns hints for restaurant')`, `it.todo('returns hints for lawyer')`, `it.todo('returns hints for consultant')`, `it.todo('returns hints for trades-general')`, `it.todo('returns hints for beauty')`, `it.todo('returns neutral fallback for unknown industry')`

4. `prompts.test.ts`:
   - Stubs: `it.todo('CODE_AGENT_SYSTEM contains mobile-responsive enforcement line')`, `it.todo('CODE_AGENT_SYSTEM requires <meta name=viewport> in every HTML file')`, `it.todo('buildCodeAgentPrompt injects {industry_hints} slot with the provided hints')`

Use vitest's `describe` + `it.todo` pattern exactly as in Phase 1 stubs (01-01-PLAN.md established this).

Do NOT use `it.skip()` with empty bodies — Phase 1 learned this can fail in strict mode (see STATE.md decision log).
  </action>
  <verify>
    <automated>npm run test:ci -- supabase/functions/_shared/html-scanner.test.ts supabase/functions/_shared/cost-calc.test.ts supabase/functions/_shared/industry-hints.test.ts supabase/functions/_shared/prompts.test.ts</automated>
  </verify>
  <done>Four files exist, all tests report as todo (skipped-todo), vitest exits 0, no import errors, no production code imported.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/test/ stubs (7 files) for GEN-01..09 and CUST-05</name>
  <files>
    src/test/gen01.test.ts,
    src/test/gen02.test.ts,
    src/test/gen03.test.ts,
    src/test/gen05.test.ts,
    src/test/gen08.test.ts,
    src/test/gen09.test.ts,
    src/test/cust05.test.ts
  </files>
  <action>
Create seven Vitest stub files in `src/test/`. Each file ~10–20 lines of `describe` + `it.todo` stubs. Do NOT import production code — the Edge Functions, hooks, and modules under test either do not exist yet or are being refactored.

1. `gen01.test.ts` — GEN-01 structured output:
   - `describe('GEN-01 — tool_choice forced tool use', () => {`
   - `  it.todo('Code Agent uses tool_choice: { type: "tool", name: "deliver_site_files" }')`
   - `  it.todo('extracts files[] from tool_use block in finalMessage response')`
   - `  it.todo('throws a clear error if no tool_use block is present (should not happen with forced tool_choice)')`
   - `  it.todo('Image Agent uses tool_choice with deliver_search_queries tool name')`
   - `})`

2. `gen02.test.ts` — GEN-02 browser-bundle scan:
   - `describe('GEN-02 — ANTHROPIC_API_KEY never in browser bundle', () => {`
   - `  it.todo('scans src/ for VITE_ANTHROPIC — must not exist')`
   - `  it.todo('scans src/ for import.meta.env.ANTHROPIC_API_KEY — must not exist')`
   - `  it.todo('scans src/ for literal "@anthropic-ai/sdk" import — must not exist (only allowed in supabase/functions/)')`
   - `})`

3. `gen03.test.ts` — GEN-03 retry ladder + per-package caps:
   - `describe('GEN-03 — per-package token caps', () => {`
   - `  it.todo('starter package uses max_tokens=12000')`
   - `  it.todo('professional package uses max_tokens=24000')`
   - `  it.todo('enterprise package uses max_tokens=48000')`
   - `})`
   - `describe('GEN-03 — retry ladder', () => {`
   - `  it.todo('retries on 429 up to 2 times with exponential backoff')`
   - `  it.todo('retries on 529 up to 2 times')`
   - `  it.todo('retries on 5xx up to 2 times')`
   - `  it.todo('does NOT retry on 400 — fails loudly immediately')`
   - `  it.todo('does NOT retry on 401/403 — fails loudly immediately')`
   - `  it.todo('caps at 2 retries total (3 attempts) then throws')`
   - `})`

4. `gen05.test.ts` — GEN-05 queue state transitions:
   - `describe('GEN-05 — client_sites queue state', () => {`
   - `  it.todo('increments retry_count on each retry')`
   - `  it.todo('sets next_retry_at to now()+backoff on retryable failure')`
   - `  it.todo('sets last_attempted_at on every attempt')`
   - `  it.todo('logs retry_scheduled to build_events with backoff ms')`
   - `})`

5. `gen08.test.ts` — Edge Function split smoke tests:
   - `describe('GEN-08 — Edge Function split exists', () => {`
   - `  it.todo('supabase/functions/generate-site/index.ts exports Deno.serve handler')`
   - `  it.todo('supabase/functions/persist-files/index.ts exports Deno.serve handler')`
   - `  it.todo('supabase/functions/build-orchestrator/index.ts exports Deno.serve handler')`
   - `  it.todo('legacy build-site/ directory is removed or stubbed')`
   - `})`

6. `gen09.test.ts` — useBuildStatus Realtime hook:
   - `describe('GEN-09 — useBuildStatus Realtime', () => {`
   - `  it.todo('subscribes to build_events postgres_changes channel filtered by site_id')`
   - `  it.todo('subscribes to client_sites UPDATE filtered by id')`
   - `  it.todo('appends INSERT payload.new to events state')`
   - `  it.todo('updates buildStatus on client_sites UPDATE')`
   - `  it.todo('starts 10s polling fallback on CHANNEL_ERROR status')`
   - `  it.todo('clears polling interval on SUBSCRIBED status')`
   - `  it.todo('unsubscribes and clears interval on unmount')`
   - `})`

7. `cust05.test.ts` — OnboardingPage race condition:
   - `describe('CUST-05 — onboarding slow signUp race', () => {`
   - `  it.todo('handleSubmit is called once after signUp resolves slowly (no setTimeout)')`
   - `  it.todo('OnboardingPage.tsx source contains no setTimeout(() => handleSubmit(), N)')`
   - `})`

All files must be valid TypeScript. Use the exact `describe.*GEN-XX` string patterns above — Wave 1 tasks will grep for them to validate the stubs were respected.
  </action>
  <verify>
    <automated>npm run test:ci -- src/test/gen01.test.ts src/test/gen02.test.ts src/test/gen03.test.ts src/test/gen05.test.ts src/test/gen08.test.ts src/test/gen09.test.ts src/test/cust05.test.ts</automated>
  </verify>
  <done>Seven files exist, all tests report as todo, vitest exits 0, no import errors, no production code imported.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite smoke + commit</name>
  <files>
    (no new files — validation of Task 1 + Task 2)
  </files>
  <action>
Run the complete Vitest suite to confirm: (a) the 11 new stub files are discovered, (b) existing Phase 1 tests still pass, (c) total test count increased by roughly 40–50 todo items (rough count from the stubs above).

Expected output shape: `Test Files  N passed (N)` where N increased by 11 compared to Phase 1 baseline, plus a large number of `todo` entries in the summary.

If any existing test now fails, STOP — the stubs broke something. Investigate before committing.

Do NOT touch `vite.config.ts` — Phase 1 already included both glob patterns. Do NOT reinstall Vitest. Do NOT create a setup file (Phase 1 already did).
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>Full suite exits 0. Phase 1's 39 tests still pass. 11 new stub files added to the discovered set. No regressions.</done>
</task>

</tasks>

<verification>
Full Phase 2 stub scaffolding is in place:
- 4 shared-module stubs alongside `_shared/` (pattern match Phase 1)
- 7 product-logic stubs in `src/test/`
- No production code imported (zero import-resolution errors)
- `npm run test:ci` exits 0
- `npm run test -- --run` exits 0
</verification>

<success_criteria>
1. 11 new test files exist with the exact paths in `files_modified`
2. Every file uses `describe` + `it.todo` (no empty `it.skip()` blocks — Phase 1 decision)
3. `npm run test -- --run` exits 0
4. The stubs contain the exact `describe` string patterns Wave 1 tasks will use for verify
5. Phase 1's existing 39 tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-generation-hardening/02-01-SUMMARY.md` following the standard SUMMARY template. Note that this plan has `requirements: []` because it is pure test scaffolding with no product requirement.
</output>
